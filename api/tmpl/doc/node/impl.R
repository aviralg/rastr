#' Create and query `_NODE_` nodes
#'
#' @param ast AST object
#' @param node `_NODE_` node
#'
#' @description
#' * `_NODE__new()` instantiates a new `_NODE_` node.
#' * `_NODE__type()` checks if the node is a `_NODE_` node.
#' * `_NODE__<child>_get()` returns the `<child>` node of `_NODE_`.
#' * `_NODE__<child>_set()` sets the new `<child>` and deletes the old `<child>`.
#' * `_NODE__<child>_rep()` sets the new `<child>` and returns the old `<child>`.
#'
#' @name doc__NODE_

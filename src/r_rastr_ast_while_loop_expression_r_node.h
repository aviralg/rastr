#include "RIncludes.hpp"

extern "C" {
SEXP r_rastr_ast_while_loop_expression_r_node_create(SEXP r_keyword,
                                                     SEXP r_condition,
                                                     SEXP r_body);
}

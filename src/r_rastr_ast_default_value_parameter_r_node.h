#include "RIncludes.hpp"

extern "C" {
SEXP r_rastr_ast_default_value_parameter_r_node_create(SEXP r_op,
                                                       SEXP r_name,
                                                       SEXP r_value);
}
